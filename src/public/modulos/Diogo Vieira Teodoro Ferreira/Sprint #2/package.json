{
  "name": "dieta-smart",
  "version": "1.0.0",
  "description": "Agenda de horários usando JSON Server",
  "scripts": {
    "start": "json-server --watch db.json --port 3000 --static ."
  },
  "devDependencies": {
    "json-server": "^0.17.3"
  }
}
